import iterMap from './iterMap'

const mockData = [
  ['k1', 'v1'],
  ['k2', 'v2'],
  ['k3', 'v3'],
  ['k4', 'v4'],
]

it('should ', function() {
  const testMap = new Map([
    ['k1', 'v1'],
    ['k2', 'v2'],
    ['k3', 'v3'],
    ['k4', 'v4'],
  ])
  // iterMap(testMap, v => v)
})
